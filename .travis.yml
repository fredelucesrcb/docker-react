sudo: required
services:
  - docker

before_install:
  - docker build -t rfredeluces/docker-react -f Dockerfile.dev .

script: 
  - docker run -e CI=true rfredeluces/docker-react npm run test --coverage